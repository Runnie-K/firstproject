using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MinJung : Human
{
    // Start is called before the first frame update
    void Start()
    {
        myName = "±ËπŒ¡ﬂ";
        print(myName);
        PowerPush();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
